# 印刷订单管理系统 - 开发记录

> 文档生成时间：2026-04-24
> 负责人：Weii
> 协作助手：Lucy (Hermes Agent)

---

## 一、项目概述

### 1.1 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  前端 (React + Ant Design)                                   │
│  /home/weii/soft/yinshua-admin/                             │
│  端口：3000 → http://10.147.19.111:3000                      │
└────────────────────┬────────────────────────────────────────┘
                     │ /api/*
┌────────────────────▼────────────────────────────────────────┐
│  后端 (Express + MSSQL)                                      │
│  /home/weii/soft/yinshua-api-node/                          │
│  端口：8000 → http://10.147.19.111:8000                      │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│  数据库 (SQL Server 2019 on NAS Docker)                      │
│  10.147.19.81:1433 / yinshua_test / sa / 1Zhihama!@         │
│  Docker 容器名: mssql-yinshua-test                           │
└─────────────────────────────────────────────────────────────┘

生产只读环境（端口 3001/8001）：
  → 连接 Windows SQL Server 192.168.0.147 / yinshua（只读）
  → 密码：Admin12345
```

### 1.2 产品线

| 代号 | 名称 | 主表 | 工序表 |
|------|------|------|--------|
| YS | 印刷单 | YS | YSGX |
| YM | 印刷面 | YM | YMGX |
| ZM | 纸盒单 | ZM | ZMGX |
| DS | 滴塑单 | DS | DSGX |

### 1.3 旧系统
- 路径：`/home/weii/soft/iis/`（ASP + SQL Server 2008）
- 登录：admin/123456
- MD5截断算法：`WordToHex(b)+WordToHex(c)`

---

## 二、网络架构与密码

| 节点 | ZT IP | LAN IP | SSH | 用途 |
|------|-------|--------|-----|------|
| 本机 Linux | 10.147.19.111 | — | — | 开发、部署 |
| NAS Synology | 10.147.19.81 | 192.168.0.251 | admin/Wei2081892 | Docker MSSQL |
| Windows SQL Server | 10.147.19.187 | 192.168.0.147 | — | 生产库 yinshua（只读） |

**关键密码：**
- 本机 sudo：`2081892`
- NAS SSH：`Wei2081892`
- NAS Docker MSSQL：`1Zhihama!@`
- Windows 生产库：`Admin12345`

---

## 三、Git 提交记录

### 后端 (yinshua-api-node)

| Commit | 时间 | 描述 |
|--------|------|------|
| `560fb06` | 2026-04-23 | fix: 修复3个严重安全问题（canReportStep未定义/JWT硬编码/不良统计SQL参数错误） |
| `0253369` | 2026-04-18 | fix(YM): 修复订单详情缺失kuanhao/fahuodanwei/jiagongfei字段 |
| `f3260df` | 2026-04-22 | fix: 精简各产品线字段列表（解决Promise.all并行查询问题） |
| `63d237c` | 2026-04-22 | feat: 调整字段列表（引入YS字段到YM/ZM/DS，导致bug，已由f3260df修复） |

### 前端 (yinshua-admin)

| Commit | 时间 | 描述 |
|--------|------|------|
|| `260506-585c078` | 2026-05-06 | 业务员下拉改为 SalesPerson 表同步（部门E，UPSERT）；标签统一 YS:印刷/YM:印唛/ZM:织唛/DS:滴塑；业务员接口改为 salespersonsList |
|| `b2dcf5d` | 2026-05-06 | fix+docs: 引用订单禁止切换Tab + 更新开发记录 |
|| `ec21dcd` | 2026-05-05 | fix: 引用订单状态下禁止切换订单类型Tab (copySource时disabled) |
|| `1c1be6b` | 2026-05-05 | ZM打印页色卡明细+尺码明细黑白灰配色；整理工序16标签 |
|| `ea3064c` | 2026-05-04 | fix(ZM打印): 整理工序16标签；8个生产规格字段；ywy_name JOIN修复 |
|| `807917a` | 2026-05-04 | YM打印页字段修正；sydazhang改为实用米数+元/米；ywy_name JOIN |
|| `a2616c1` | 2026-05-04 | fix: PrintYsPage删除底部订单编号一行（订单编号不要显示） |
|| `34527a0` | 2026-05-04 | fix: YM前端工序hzl4-7顺序与旧系统一致；后端YM INSERT新增yss20；zhengli UPDATE |
|| `aa25e81` | 2026-05-04 | fix: adminCreateOrder改用/api/order-entry；OrderDetailPage印件分析显示全部行 |
|| `903fab8` | 2026-05-03 | fix(OrderEntryPage): fetch改为api实例(axios)；印件分析标签改为旧系统名称 |
|| `67e57dd` | 2026-05-02 | 更新版本号 |
|| `1d8edde` | 2026-05-01 | fix(PrintYsPage): 删除印机型号/发料日期/订单编号标题行；@page margin:0 |
|| `b035516` | 2026-04-29 | 更新版本号 |
|| `1eb0111` | 2026-04-28 | feat: PrintYsPage增加CODE128条形码 |
|| `a8b3534` | 2026-04-28 | fix: 工艺分析分类显示 |
|| `92caa94` | 2026-04-23 | fix: LoginPage 修复 Open Redirect 漏洞 |
|| — | 2026-04-24 | fix: YM_PROCESS_LIST 对齐表单checkbox；YM表单加加工费字段 |

---

## 四、已完成功能

### 核心功能
- ✅ 登录认证（支持旧系统MD5截断格式）
- ✅ 首页看板（Dashboard）
- ✅ 订单列表/详情/录入（YS/YM/ZM/DS）
- ✅ 综合查询（含导出Excel）
- ✅ 工序进度（JHK）
- ✅ 发货单管理（FH）

### 系统功能
- ✅ JWT 统一配置中心（启动校验）
- ✅ 数据库配置从环境变量读取
- ✅ 前端 API_BASE 改为相对路径（解决跨环境hardcode问题）
- ✅ NAS Docker 后端连接 yinshua_test（而非生产库）

---

## 五、已修复 Bug 记录

### Bug #1：admin.js 字段列表套用错误（严重）
**发现时间：** 2026-04-22
**Commit：** `63d237c`（引入）→ `f3260df`（修复）

**问题：** commit 63d237c 把 YS 表的字段列表套到了 YM/ZM/DS 表上，但这些表**缺少 YS 独有的列**：
- `klyaoqiu`、`beizhuYS`、`klcc`、`kaishu`、`xukaisl`、`bcsl`、`yjbhao`、`cpgg`、`pingshu` 等

这导致 `Promise.all` 并行查询时，一个表失败全部挂掉。

**修复：** 精简各产品线字段列表，每个表只查自己实际存在的字段。

---

### Bug #2：YM 订单详情字段缺失
**发现时间：** 2026-04-18
**Commit：** `0253369`

**问题：**
1. `admin.js` 的 `buildProgress` 函数 YM 分支缺少：`kuanhao`、`fahuodanwei`、`jiagongfei`、`waifa`
2. `order_entry.js` YM INSERT 使用了不存在的字段 `yssj`，应为 `yjbhao`

**修复：**
- 后端 `buildProgress` YM 分支补全字段
- 后端 `order_entry.js` YM INSERT 修正 `yssj` → `yjbhao`

---

### Bug #3：前端 API_BASE Hardcode 导致连错数据库
**发现时间：** 2026-04-22

**问题：**
- 前端 `src/api/index.js` 里 `API_BASE` 硬编码为 `http://10.147.19.81:8000`
- 本机前端 `.env` 不存在，fallback 到 hardcode 值
- 结果：即使本机后端运行在 `:8000`，前端仍请求 NAS 后端 `:8000`
- NAS 后端连接的是 `192.168.0.147` 的**生产库 yinshua**（751742条），而非测试库（248389条）

**修复：**
- 前端 `API_BASE` 改为 `''`（相对路径）
- NAS 后端 `docker-compose.yml` 改为连接 Docker 内部 `sqlserver` 的 `yinshua_test`
- 验证：本地 `:8000` 和 NAS `:8000` 均返回 `248389` ✅

---

### Bug #4：YM PROCESS_LIST 和表单 checkbox 不对齐
**发现时间：** 2026-04-24

**问题：** `YM_PROCESS_LIST` 和前端 YM tab 表单 checkbox 标签完全对不上：

| hzl | 表单 checkbox 实际标签 | YM_PROCESS_LIST（错误） |
|-----|----------------------|------------------------|
| hzl1 | 烘色牢度 | 晒版 |
| hzl2 | 切割 | 显影 |
| hzl3 | 超声波切割 | 烘版 |
| hzl4 | 三角折 | 拼版 |
| hzl5 | 手工切折 | 擦版 |
| hzl6 | 手工对折 | 贴膜 |
| hzl7 | 其它 | 打包 |

这导致用户选择工序后，提交时 `sclcSteps` 数组内容和表单显示不一致。

**修复：**
- `YM_PROCESS_LIST` 改为 `['烘色牢度', '切割', '超声波切割', '三角折', '手工切折', '手工对折', '其它']`
- YM 表单增加 `加工费(jiagongfei)` 输入框

---

### Bug #5：NAS Docker 数据库连接配置错误
**问题：** docker-compose.yml 里 `DB_SERVER=192.168.0.147` 连接的是 Windows 生产库

**修复：** 改为 `DB_SERVER=sqlserver`（Docker 内部网络）+ `DB_NAME=yinshua_test` + `DB_PASSWORD=1Zhihama!@`

---

### Bug #6：OrderEntryPage 缺少 adminListUsers import
**时间：** 2026-04-24
**文件：** `src/pages/OrderEntryPage.jsx`

**问题：** 第136行调用 `adminListUsers()` 但文件顶部没有引入该函数，导致页面崩溃（Uncaught ReferenceError）

**修复：** 在 import 语句中添加 `adminListUsers`：
```diff
-import { adminCreateOrder } from '../api';
+import { adminCreateOrder, adminListUsers } from '../api';
```

---

## 六、安全修复记录

### 安全问题 #1：canReportStep 函数未定义就调用
**Commit：** `560fb06`
**位置：** `routes/production.js:364`

### 安全问题 #2：不良统计 SQL 参数索引错误
**Commit：** `560fb06`
**位置：** `routes/production.js:711`
**问题：** `params.length` 在条件分支后不再准确，改用独立 `totalParams` 计数

### 安全问题 #3：JWT_SECRET 硬编码 fallback 值
**Commit：** `560fb06`
**位置：** `routes/auth.js`, `middleware/auth.js`, `routes/users.js`
**问题：** 代码里有硬编码的默认 JWT_SECRET，攻击者可用其伪造有效 JWT

### 安全问题 #4：Open Redirect 漏洞
**Commit：** `92caa94`
**位置：** `src/pages/LoginPage.jsx`
**问题：** 未验证 `redirectTo` 参数，攻击者可构造恶意链接跳转外部网站
**修复：** 添加白名单校验 `redirectTo.startsWith('/')`，否则重置为 `/`

---

## 七、代码审查遗留问题

> 审查时间：2026-04-23
> 文档位置：`/tmp/yinshua-remaining-issues.md`

### 严重问题：0项（已全部修复）

### 中等问题（19项）— 需近期处理

| # | 问题 | 位置 | 优先级 |
|---|------|------|--------|
| 1 | SQL动态表名拼接 | routes/orders.js, admin.js | 高 |
| 2 | CORS允许所有来源 | server.js | 中 |
| 3 | 多处静默catch吞掉错误 | 各页面 | 高 |
| 4 | XSS打印风险（用户数据直接拼HTML） | src/utils/print.js | 高 |
| 5 | selectedKeys状态错误（函数式setState） | FahuoListPage.jsx | 中 |
| 6 | keyFor vs keyForOrder拼写不一致 | FahuoListPage.jsx | 低 |
| 7 | useEffect依赖遗漏 | OrderEntryPage.jsx | 中 |
| 8 | atob解码不够标准 | OrderEntryPage.jsx | 低 |
| 9 | 产品线常量重复定义（5+处） | 多个页面 | 中 |
| 10 | 日期格式化函数重复实现 | 各页面 | 低 |
| 11 | AppLayout每次渲染都解析localStorage | AppLayout.jsx | 低 |
| 12 | Dashboard Promise.all一个失败全挂 | DashboardPage.jsx | 中 |
| 13 | 报工数量上限硬编码1.1 | ProductionPage.jsx | 低 |
| 14 | 工序步骤字段名定义冲突（jhkdd vs jhkddClass） | 多个路由 | 中 |
| 15 | 审计日志异步写入无重试 | middleware/audit.js | 低 |
| 16 | 数据库连接池无健康检查 | config/db.js | 低 |
| 17 | WorkerID类型不一致 | routes/production.js | 低 |
| 18 | fetch绕过axios拦截器（已确认非问题） | OrderEntryPage.jsx | — |

### 改进建议（15项）— 后续安排

| # | 建议 | 说明 |
|---|------|------|
| 1 | 密码升级到bcrypt | 当前MD5截断不安全 |
| 2 | 敏感操作添加工序者验证 | routes/admin.js |
| 3 | 订单号ddbh并发生成问题 | 高并发可能重复 |
| 4 | 添加请求频率限制 | 防止暴力破解 |
| 5 | API统一响应格式 | 消除{error:}和{success:}混用 |
| 6 | 结构化日志 | 引入winston/pino |
| 7 | Swagger暴露生产地址 | 从环境变量配置 |
| 8 | 缺少数据库迁移工具 | 引入flyway |
| 9 | models/目录为空 | 无DAL层 |
| 10 | 导出大数据无分页 | 一次性加载 |
| 11 | 表单提交无并发保护 | 快速双击多次请求 |
| 12 | 用户角色显示不完整 | 未知角色统一显示"用户" |
| 13 | 导出按钮无明确失败反馈 | QueryPage.jsx |
| 14 | 无输入验证中间件未充分利用 | 统一使用validate() |
| 15 | UserInfo表结构与UserID类型不明确 | routes/users.js |

---

## 八、2026-04-24 修改记录

### Bug #4（补充）：YM PROCESS_LIST 和表单 checkbox 不对齐
**文件：** `src/pages/OrderEntryPage.jsx`

**问题：** `YM_PROCESS_LIST` 标签和表单实际 checkbox 完全对不上，导致提交工序和显示不一致；YM 表单缺 `加工费(jiagongfei)` 输入框

**修复：**
- `YM_PROCESS_LIST` 改为 `['烘色牢度', '切割', '超声波切割', '三角折', '手工切折', '手工对折', '其它']`
- YM 表单增加 `加工费(jiagongfei)` 输入框（款号右侧）

### Bug #6：OrderEntryPage 缺少 adminListUsers import
**文件：** `src/pages/OrderEntryPage.jsx`

**问题：** 第136行调用 `adminListUsers()` 但文件顶部没有引入该函数，导致页面崩溃（Uncaught ReferenceError）

**修复：** 添加 `adminListUsers` 到 import 语句

### Bug #7：OrderListPage 缺少 PRODUCT_BG import
**文件：** `src/pages/OrderListPage.jsx`

**问题：** `OrderCard` 组件第161行使用 `PRODUCT_BG` 但 import 中没有引入，导致页面崩溃

**修复：** 添加 `PRODUCT_BG` 到 import 语句

### 功能：YS/YM 详情页印件总价分析显示总价元/只行
**文件：** `src/pages/OrderDetailPage.jsx`

**问题：** YS/YM 详情页的印件总价分析表格没有显示"总价 元/只"汇总行

**修复：**
- YS 详情：在印件总价分析表格末尾加一行 `<tr style={{background:'#e8f4fd',fontWeight:600}}><td>总价 元/只</td><td colSpan="2">order.yszj</td></tr>`
- YM 详情：同样加一行，样式背景 `#e8f4fd`
- `ymAnalysisRowHasValue`：有 `yl` 字段才检查，避免 `order[item.yl]` 取到 undefined

**版本：** `26042423-744ba07`

### 功能：YM 订单录入保存 jine1-9 数据
**文件：** `routes/order_entry.js`

**问题：** `order_entry.js` 的保存逻辑只处理了 YS 的 jine1-9，YM 根本没有写入数据库，导致 YM 表里 `jine1-9` 全部为 NULL，详情页印件总价分析无法显示

**修复：** 在 YS 块后面加 YM 的保存逻辑：
```javascript
if (product_type === 'YM') {
  const updateParts = [];
  const updateParams = [];
  let pi = 0;
  for (let i = 1; i <= 9; i++) {
    if (body[`yssl${i}`] !== undefined || body[`jine${i}`] !== undefined) {
      updateParts.push(`yssl${i}=@p${pi}`, `jine${i}=@p${pi+1}`);
      updateParams.push([`p${pi}`, db.mssql.NVarChar, String(body[`yssl${i}`] || '')], [`p${pi+1}`, db.mssql.NVarChar, String(body[`jine${i}`] || '')]);
      pi += 2;
    }
  }
  if (body.yszj !== undefined) {
    updateParts.push(`yszj=@p${pi}`);
    updateParams.push([`p${pi}`, db.mssql.NVarChar, String(body.yszj || '')]);
    pi += 1;
  }
  if (updateParts.length > 0) {
    updateParams.push([`p${pi}`, db.mssql.Int, newId]);
    const updateSql = `UPDATE ${product_type} SET ${updateParts.join(',')} WHERE DD_id=@p${pi}`;
    await reqT(updateSql, updateParams);
  }
}
```

**版本：** `26042423-744ba07`（前端 `OrderDetailPage.jsx` + 后端 `order_entry.js`）

---

## 九、待完成功能

- ⬜ 发货单打印/导出
- ⬜ 车间订单修改（JHK）完善
- ⬜ 统计报表页面
- ⬜ 打印功能完善
- ⬜ 用户管理页面
- ⬜ 手机端适配
- ⬜ 问题反馈功能（wenti）
- ⬜ YM/DS 录入实测验证

---

## 十、NAS 部署指南

> 部署路径：`/volume1/docker/yinshua/`

**部署流程：**
1. 本机修改代码 → `git push` 到 GitHub
2. SSH 到 NAS：`sshpass -p 'Wei2081892' ssh admin@10.147.19.81`
3. 在 NAS 上：`cd /volume1/docker/yinshua`
4. 拉取代码：`git pull`
5. 重新构建：`docker-compose build backend`（或 `frontend`）
6. 重启容器：`docker-compose up -d`

**注意：** docker-compose.yml 里的数据库配置是 hardcode 的，代码同步后需手动更新，或改用 `.env` 管理。

---

## 十一、数据库表结构速查

### YM 表关键字段
```
DD_id, ddbh, prouddate, overdate, company, yjbhao, cpgg, pingshu,
ylzd, shuliang, ywy, sydazhang, danjia, syMoney, jyyaoqiu,
zhengli, gyyq, zhidan, proudnumber, lldate, yszj,
fahuodanwei, kuanhao, jiagongfei, waifa,
beizhu8, beizhuYM, UpFile, BZ, ZT, fahuo
```

### YM 工序（hzl1-hzl7）
```
hzl1-烘色牢度, hzl2-切割, hzl3-超声波切割,
hzl4-三角折, hzl5-手工切折, hzl6-手工对折, hzl7-其它
```

### YS 工序（jhkddClass 映射）
```
2=印刷单, 3=客户印
```

---

## 2026-04-25 YM 后整理工艺（hzl1-7）修复

### 问题现象
- YM 订单录入时勾选"后整理工艺"（如烘色牢度、切割等），提交后工序数据未保存
- YM 订单详情页不显示 hzl1-7 数据

### 根因分析
1. **数据库层**：hzl1-7 列存在于 **YMGX 工序表**，而非 YM 主表。YM 主表没有这些列。
2. **前端映射错误**：`OrderEntryPage.jsx` 第179行 YM 分支的 sclcSteps 映射用了 `YM_PROCESS_LIST`（中文值列表）去匹配 stepsMap key（`hzl1`），永远匹配不上，导致 sclcSteps 永远是空数组。
3. **后端字段列表错误**：`admin.js` 第369行 YM 字段列表里错误地加了不存在的 `hzl1-7` 列，导致 YM 查询全部报错 `Invalid column name` 返回空数据。

### 修复内容
| 文件 | 修复 |
|------|------|
| `admin.js` 第369行 | 移除 YM 字段列表中不存在的 hzl1-7 列 |
| `admin.js` 第407行 | 修正 YMGX IN 查询参数格式 |
| `OrderEntryPage.jsx` 第179行 | YM sclcSteps 改用 `Object.keys(STEPS_MAP_YMGX).indexOf(field)` 反查索引 |
| `handleStepToggle` YM 分支 | 确认已有 `setStepsMap(updated)` 调用 |

### 验证结果
- 测试订单 241573：sclcSteps=["hzl1-晒版","hzl2-显影"]，数据库 YMGX hzl1=1, hzl2=1 ✅
- API 详情查询返回 hzl1=1, hzl2=1 ✅
- 旧订单 2604250018/2604250019/2604250020 无数据为历史原因（修复前创建），非代码问题

### Git
- yinshua-admin: f02131d（feature/user-management）
- yinshua-api-node: 9350a55（feature/user-management）

---

## 2026-04-25 下午 YM 详情页空白 + 新建订单后整理工艺丢失 修复

### 问题一：订单详情页小写 ym 空白

**现象：** URL 小写 `orders/ym/241576` 打开后基本信息区域完全空白，但大写 `orders/YM/241576` 正常。

**根因：** `OrderDetailPage.jsx` 第510行 `const { productType, ddId } = useParams()` 直接使用 URL 参数原始值（小写 `ym`），然后和 `'YM'` 做 `===` 比较永远不匹配，导致 `YMOrderDetail` 组件不渲染。

**修复：** `const productType = rawProductType.toUpperCase()`，同时 API 调用改用 `rawProductType`（后端自动转大写）。

### 问题二：新建 YM 订单勾选后整理工艺不保存

**现象：** 新建 YM 订单时勾选"后整理工艺"（烘色牢度、切割等），提交后详情页不显示。

**根因：** `OrderEntryPage.jsx` 第521行 YM 后整理工艺复选框的 `Checkbox` 没有 `onChange` 处理，只更新了表单字段但没有更新 `stepsMap`，导致 `handleCreate` 中 `sclcSteps` 构建时 `stepsMap` 是空的。

**修复：** `Checkbox` 添加 `onChange: () => handleStepToggle('YM', opt.value)`。

### 修复文件

| 文件 | 修改内容 |
|------|----------|
| `src/pages/OrderDetailPage.jsx` | `useParams` 取值后统一 `toUpperCase()`，API 调用用原始值 |
| `src/pages/OrderEntryPage.jsx` | YM 后整理工艺 Checkbox 添加 `onChange={handleStepToggle('YM', ...)}` |

### Git
- yinshua-admin: a6ce65a（feature/user-management）✅ 已 push
- yinshua-api-node: 9350a55（feature/user-management）✅ 已 push

## 2026-04-25 订单编辑保存修复

### 问题
订单编辑页保存失败，浏览器控制台报错：
1. `Invalid column name 'sclcClass'`（YS表没有sclcClass列）
2. `Invalid column name 'beizhu'`（YS表没有beizhu列，只有beizhuYS）
3. `Invalid column name 'hzl7'`（YS表没有hzl1-7列）

### 根因
1. 后端ALLOWED_FIELDS是通用大数组，包含所有产品线字段，YM提交时混入了YS/ZM/DS特有字段
2. formatOrderResponse返回`beizhu: order.beizhuYS`，但PATCH时前端发送`beizhu`作key，后端直接当列名用
3. 工序字段（sclcClass/hzl1-7）在各产品线的GX表，不在主表

### 修复内容
**后端 admin.js:**
- ALLOWED_FIELDS从通用数组改为按YS/YM/ZM/DS三元表达式分别定义
- YS移除: sclcClass, hzl1-7, beizhu1-5, beizhu8, kaishu, xukaisl, bcsl等
- YM移除: hzl1-7, beizhu1-5等工序/不存在列
- 添加FIELD_MAP: beizhu→beizhuYS(YS), beizhu→beizhuYM(YM)
- 所有updates.push改用dbCol(数据库列名)而非key(JSON字段名)
- YMGX工序表更新逻辑(第726-873行)

**前端 OrderEntryPage.jsx:**
- YS/YM requiredFields分离，YM编辑模式移除sclcClass
- YS/YM表单step=0.0001(金额精度)
- calcTotal合并为product参数区分的单一函数
- 修复API数据提取兜底
- 支持编辑模式(编辑/录入切换)

**前端 OrderDetailPage.jsx:**
- 添加编辑按钮(jhkprint权限校验)

**前端 App.jsx:**
- 添加/orders/edit/:productType/:ddId路由

### commit
- yinshua-api-node: `2233246` fix: ALLOWED_FIELDS按产品线分开+FIELD_MAP字段映射+dbCol用于SQL列名
- yinshua-admin: `5b0afd2` fix: 订单编辑保存权限+requiredFields按产品线分开+字段精度


## 2026-04-26 YS钉单工艺配置修复

### 问题
YS订单编辑页，工艺配置勾选后保存，订单信息里不显示勾选内容。

### 根因
1. **GET**: `sclcClass`/`hzlA*/hzlB*/hzlC* 存在 YSGX 表，但 GET 只查 YS 主表，没查 YSGX
2. **buildProgress**: YS 返回块没有包含 hzlA*/hzlB*/hzlC* 字段，即使 order 对象有数据也没返回给前端
3. **PATCH**: YSGX 更新逻辑不存在，且 YSGX 记录可能不存在（UPSERT 问题）

### 修复内容
**admin.js GET 路由:**
- YS: 查 YSGX 表 sclcClass/GXS/hzlA*/hzlB*/hzlC* 用 Object.assign 合并到 order

**admin.js buildProgress:**
- YS 返回块增加 GXS + 所有 hzlA*/hzlB*/hzlC* 字段

**admin.js PATCH 路由:**
- YS YSGX UPSERT: 检查记录是否存在，不存在则 INSERT，存在则 UPDATE
- sclcClass 单独处理（数字 1=纸盒 2=印刷单）
- sclcSteps 数组批量处理 hzlA*/hzlB*/hzlC* 工序勾选

### commit
- yinshua-api-node: `6a730d9` fix: YS工艺配置读写YSGX表+UPSERT+GET返回
- yinshua-admin: `c433ae3` fix: 编辑模式禁用产品类型Tab切换


---

## 2026-04-26 下午 YS 工艺配置编辑保存修复（第二轮）

### 背景
上一轮（上午）修复了：GET 读 YSGX 表、buildProgress YS 块补全字段、PATCH 加 YSGX UPSERT 逻辑。但用户测试发现：详情页能显示了，编辑后保存仍然不生效。

### Bug A：前端详情页显示层 — OPTIONS value 用错字段
**症状：** API 返回 `hzlA1: 1` 正确，但详情页工艺配置不显示。

**根因：** `OrderDetailPage.jsx` 的 `TIEMO_OPTIONS`/`CHANGGUI_OPTIONS` 的 `value` 用的是 `tmG`/`cgJG` 等（数据库不存在的列），而 YSGX 实际列名是 `hzlA*`/`hzlB*`。

**修复：** `TIEMO_OPTIONS` value 改为 `hzlA1-6/hzlC1/3/6-9`，`CHANGGUI_OPTIONS` value 改为 `hzlB3-15/hzlC4/5/10`，`hasAnyCraft` 改为检查 YSGX 实际字段。

### Bug B：后端 PATCH 路由 — `updates.length === 0` 提前返回
**症状：** 只改工艺配置时 PATCH 返回 `{"error":"没有有效的更新字段"}`，YSGX UPSERT 代码根本没执行。

**根因：** PATCH 路由在第856行检查 `if (updates.length === 0) return 400`。YS 的 `sclcClass`/`sclcSteps` 不在 ALLOWED_FIELDS 里（全走 YSGX UPSERT），当用户只改工艺不改其他字段时，`updates.length === 0` 直接 return，后面的 YSGX UPSERT 代码被跳过。

**修复：** 将 `updates.length === 0` 改为 `updates.length === 0 && product_type !== 'YS' && !(product_type === 'YM' && ...)`，让 YS/YM 至少能进入 YSGX UPSERT 逻辑。同时 `if (updates.length > 0)` 才执行主表 UPDATE。

### commit
- yinshua-api-node: `6a730d9`（上午：YSGX读写+UPSERT）→ `PATCH updates.length === 0 提前返回bug`
- yinshua-admin: `c433ae3`（上午：Tab禁用）→ `TIEMO/CHANGGUI OPTIONS value 修正`

## 2026-04-27 凌晨 — YS工艺配置标签校正 + hzlC3分类修复
### 问题
YS订单工艺配置，特殊工艺中勾选的内容会在贴膜区分组里显示。

### 根因
新系统工艺选项的标签（中文名）是拍脑袋写的，和旧系统源码对不上。hzlC3本应在"贴膜"分组（旧系统里是贴在贴膜区的"外加工上光"），但新系统把它放到了"特殊工艺"并标为"过油磨光"。

### 修复内容
1. OrderEntryPage.jsx 和 OrderDetailPage.jsx 的 TIEMO_OPTIONS/CHANGGUI_OPTIONS/TESHU_OPTIONS 全部按旧系统 YSinput_Add.asp 源码校正标签
2. hzlC3(外加工上光)从 TESHU_OPTIONS 移至 TIEMO_OPTIONS
3. YSGX 表实际不存在 hzlB9/hzlB10/hzlC2，已从 OPTIONS 中移除

### 工艺分类（来源：旧系统 YSinput_Add.asp）
贴膜: hzlA1-6(单面光膜/单面亚膜/双面光膜/双面亚膜/单面专用膜/双面专用膜) + hzlC3(外加工上光)
常规工艺: hzlB3-8/11-15(烫金/压钢刀/穿线/糊纸粘合/打汽眼/凹凸/激光切割/穿别针/路线/敲柳钉/包边)
特殊工艺: hzlC1/4-10(局部丝网印/绣花/烫钻/胶印上光/粘备用袋/揉皱/敲毛边/其它)

### commit
- yinshua-admin: 6fb7ca8 fix: YS工艺配置标签按旧系统源码校正，hzlC3(外加工上光)移至贴膜分组
- yinshua-api-node: aa01f22 fix: PATCH路由修复updates.length==0检查，YS工艺走YSGX UPSERT

## 2026-04-27 凌晨 — ALLOWED_FIELDS 字段补全（对比旧系统4个_Input_Add.asp源码）
### 问题
新系统 PATCH 路由的 ALLOWED_FIELDS 白名单遗漏了大量旧系统实际使用的字段，导致前端提交的数据无法写入数据库。

### 修复内容
1. ZM ALLOWED_FIELDS 补全：chenpingcc/kuandu/changdu/huachang/kts/dhdw/cmh1~10/sl1~10/lieshu1~10（来源：ZMinput_Add.asp）
2. DS ALLOWED_FIELDS：移除废弃字段 cidiehao（2010/12/27旧系统已去除），新增 beizhuDS + FIELD_MAP 映射
3. YM/DS 已有 beizhu8 字段（已确认存在）

### commit
- yinshua-api-node: 821981d fix: ALLOWED_FIELDS补全ZM缺失字段+移除DS废弃cidiehao+DS beizhuDS映射

---

## 2026-04-28 ZM工序保存问题修复

### 问题
ZM订单编辑时勾选全部工序（hzl1-16），PATCH返回200成功，但详情页只显示hzl1/hzl5（切折/超声波）。

### 调试过程
1. 后端加console.log DEBUG，发现sclcSteps始终为空数组`[]`，但body里hzl1-16字段都有值
2. 发现三层原因：
   - `handleStepToggle` ZM分支没调用`setStepsMap(zmSteps)`，stepsMap永远为空
   - `sclcSteps`构建用`ZM_PROCESS_LIST.indexOf`只能处理7项，hzl8-16返回-1
   - Vite HMR对React state修复不生效，前端state仍是旧值
3. 最终解法：**后端直接读req.body['hzl'+i]**，绕过前端sclcSteps中间层

### 修复内容
1. `OrderEntryPage.jsx` `handleStepToggle` ZM分支：添加`setStepsMap(updated)`
2. `OrderEntryPage.jsx` sclcSteps构建：ZM分支改用`field.replace(/[^0-9]/g,'')`直接提取编号
3. `admin.js` ZMGX更新逻辑：去掉`sclcSteps`依赖，改为`req.body['hzl'+i]`直接读取
4. `admin.js` early return条件：ZM单独豁免，避免无其他字段更新时返回400

### ZM架构要点
- hzl1-7在ZM主表有对应中文名（ZM_PROCESS_LIST），hzl8-16仅在GongXuDefine表有定义
- ZMGX表：DD_id + hzl1-16（16个bit字段），与YSGX/YMGX/DSGX并列
- 详情页`STEPS_MAP_ZM_PROCESS`只定义了hzl1-7，hzl8-16无中文标签（需后续补全）

### ZM工序保存（hzl8-16）修复 — 2026-04-28
**问题1**：ZM订单编辑时勾选hzl8-16工序，PATCH返回200但数据库ZMGX表未写入
**根因1**：前端`handleStepToggle` ZM分支只更新`zmSteps`未调用`setStepsMap`，导致`sclcSteps`一直为空数组；后端ZM UPSERT依赖`sclcSteps`非空才执行
**修复1**：
1. 后端`admin.js` ZMGX UPSERT逻辑：去掉`sclcSteps`依赖，改为直接从`req.body['hzl'+i]`读取hzl1-16字段值
2. early return条件：ZM单独豁免（允许只有hzl1-16更新时正常保存）
3. 前端`handleStepToggle` ZM分支：补加`setStepsMap(updated)`
4. 前端`sclcSteps`构建ZM分支：改为直接提取field中的编号（`field.replace(/[^0-9]/g,'')`），不再依赖ZM_PROCESS_LIST的indexOf

**问题2**：保存后详情页显示16个工序，但再次编辑时只显示前7个
**根因2**：`setFieldsValue`字段列表缺少hzl8-16，导致表单复选框未回填；加载后未调用`setStepsMap(zmMap)`恢复勾选状态
**修复2**：
1. `setFieldsValue`字段列表：增加`hzl8`~`hzl16`
2. ZM加载工序后：补加`setStepsMap(zmMap)`
3. 后端GET路由ZM分支：补JOIN ZMGX返回hzl1-16
4. buildProgress ZM分支：补全hzl1-16字段

**Git提交**：
- yinshua-api-node: `fix(ZM): ZMGX UPSERT直接读body hzl1-16字段 + GET JOIN ZMGX + 重建ALLOWED_FIELDS白名单`
- yinshua-admin: `fix(ZM): 工序编辑加载/保存全链路修复`
**验证**：数据库ZMGX hzl1-16全部=1，详情页显示16个工序，编辑加载显示16个勾选 ✅

**YS/YM 总计行按钮UI优化**（260428）
- 按钮从 label 同行拆出，单占一格，与总价字段拉开距离
- 字体加大至 12px 加粗，24px 高，圆角 4px，带背景色和边框，造型明显
- YS「计算」蓝底蓝字、「编辑」红底红字；YM「计算」青绿底青绿字、「编辑」红底红字
- 整体风格与页面蓝青主色调协调

### 相关文件
- `/home/weii/soft/yinshua-api-node/routes/admin.js` — ZMGX UPSERT + early return
- `/home/weii/soft/yinshua-admin/src/pages/OrderEntryPage.jsx` — handleStepToggle + sclcSteps构建

---

## 2025-04-29 查询下单功能重构

### 背景
用户反馈「新建订单」点开直接弹出查询对话框不够灵活，希望有独立的查询入口。

### 改动内容

**前端（yinshua-admin）**

1. **新建/查询入口分离**
   - 侧边栏「新建订单」→ 直接打开空白录入页（`/orders/new`）
   - 侧边栏新增「查询下单」→ 打开独立查询页（`/query-order`）
   - Dashboard「新建订单」按钮保持不变

2. **新建查询下单页面** `QueryOrderPage.jsx`
   - 独立页面，Row/Col 两行布局（与综合查询页面风格统一）
   - 第一行：产品类型 + 公司名称 + 动态字段（YS/YM显示印件编号+款号，ZM显示花号+生产机型）
   - 第二行：制单日期范围 + 查询按钮
   - 支持模糊查询（后端 LIKE，已处理 % 和 _ 转义）
   - 初始进入自动加载一次数据
   - 点击行跳转 `/orders/new?copyFrom=DD_id&productType=XX`，OrderEntryPage 读取参数自动预填

3. **OrderEntryPage 改造**
   - 移除新建订单时自动弹出查询对话框的 useEffect
   - 新增 URL 参数 `copyFrom` / `productType` 读取逻辑，触发时自动加载源单数据预填表单

4. **路由**
   - `App.jsx` 新增 `/query-order` 路由 → `QueryOrderPage`

5. **表头列显示**
   - YS/YM 最后一列显示：总价（`yszj`）
   - ZM 最后一列显示：基价（`jijia`）
   - 单位列已移除

**后端（yinshua-api-node）**

1. `routes/order_entry.js` `copy-list` API
   - ZM 表字段 `yszj` → 改为 `jijia`（COPY_LIST_FIELDS 修正）
   - 所有 LIKE 查询参数转义 `%` 和 `_`，避免 SQL 通配符干扰

### 涉及文件
- `src/App.jsx` — 新增路由
- `src/components/AppLayout.jsx` — 侧边栏新增「查询下单」
- `src/pages/QueryOrderPage.jsx` — 新建文件
- `src/pages/OrderEntryPage.jsx` — 移除自动弹窗，新增 copyFrom URL 参数处理
- `routes/order_entry.js` — ZM 字段修正，LIKE 参数转义


## 2025-05-01 凌晨会话

### 1. OrderEntryPage.jsx - 第二处 YS 录入"品名"字段修复
- **问题**：之前用 `replace_all` 把两处 YS "品名" 都绑到 `jiagongfei`，导致第二处 YS 录入缺少"机印型号"
- **修复**：在第二处 YS 表单新增 `label="机印型号" name="proudnumber"` 字段
- **文件**：`/home/weii/soft/yinshua-admin/src/pages/OrderEntryPage.jsx`

### 2. OrderDetailPage.jsx - 3处"品名"绑定修正
- **问题**："品名"绑到了 `proudnumber`，但实际应显示 `jiagongfei`
- **修复**：3处 `value={order.proudnumber}` → `value={order.jiagongfei}`
- **文件**：`/home/weii/soft/yinshua-admin/src/pages/OrderDetailPage.jsx`

### 3. PrintYsPage.jsx - 工艺分析分类显示
- **问题**：工艺分析绑到 `zhengli` 字段（空白），实际工艺 checkbox 存在 YSGX 表的 hzlA/hzlB/hzlC 字段
- **修复**：
  - 新增 `gyxnA`（贴膜：hzlA1-6 + hzlC3）、`gyxnB`（常规工艺：hzlB3-15）、`gyxnC`（特殊工艺：hzlC1,4-10）
  - 显示改为分类：`贴膜：xxx`、`常规工艺：xxx`、`特殊工艺：xxx`
- **字段映射**：
  - hzlA1=单面光膜、hzlA2=单面亚膜、hzlA3=双面光膜、hzlA4=双面亚膜、hzlA5=单面专用膜、hzlA6=双面专用膜、hzlC3=外加工上光
  - hzlB3=烫金、hzlB4=压钢刀、hzlB5=穿线、hzlB6=糊纸粘合、hzlB7=打汽眼、hzlB8=凹凸、hzlB11=激光切割、hzlB12=穿别针、hzlB13=路线、hzlB14=敲柳钉、hzlB15=包边
  - hzlC1=局部丝网印、hzlC4=绣花、hzlC5=烫钻、hzlC6=胶印上光、hzlC7=粘备用袋、hzlC8=揉皱、hzlC9=敲毛边、hzlC10=其它
- **文件**：`/home/weii/soft/yinshua-admin/src/pages/PrintYsPage.jsx`

## 2026-05-01 下午
### 问题：提交订单显示 Request failed with status code 500
- 根因：`src/api/index.js` 第70行 `adminCreateOrder` 调用了错误的 API 路径 `/api/admin/orders`（该端点只支持 GET/PATCH，不支持 POST）
- 正确路径：`/api/order-entry`（`routes/order_entry.js` POST 端点）
- 修复：`api.post('/api/admin/orders', data)` → `api.post('/api/order-entry', data)`
- 受影响操作：新建订单提交（编辑走 PATCH 不受影响）
- 调试方法：用 curl 直接调 API 排除网络问题，找到 500 的真实原因
- 新构建：`index-B3V1nwTT.js`，已同步到 10.147.19.111:3000 和 NAS Docker

### PrintYsPage 标签修改
- 旧系统源码确认：YS 印件分析标签顺序为 软片/印工/PS版/铜锌版/电化铝/钢刀/轧钢刀/贴塑双(单)面/UV/切刀打洞/圆角穿线/整理包扎
- `PrintYsPage.jsx` 10行标签全部更新为旧系统名称
- 字段映射：yssl1→jine1, yssl2→jine2, ..., yssl9→jine9, yss20→jine10
- 新构建：`index-DkVKI239.js`，已同步两环境

### PrintYsPage 还原（之前的修改回退）
- 之前的4个删除修改"效果不对"，已还原 PrintYsPage.jsx


## 2026-05-02 修复：提交订单500错误 + 打印页标签修正

### 问题1：提交订单显示 Request failed with status code 500
- **根因**：`src/api/index.js` 中 `adminCreateOrder` 调用了错误的 API 路径 `/api/admin/orders`，正确路径是 `/api/order-entry`
- **修复**：修改 `adminCreateOrder` 为 `api.post('/api/order-entry', data)`
- **影响文件**：`src/api/index.js`

### 问题2：PrintYsPage 印件分析标签全部错误
- **根因**：旧系统源码（`print_ys.asp`）中印件分析标签为 软片/印工/PS版/铜锌版/电化铝/钢刀/轧钢刀/贴塑双(单)面/UV/切刀打洞/圆角穿线/整理包扎，PrintYsPage 错写成 色1/印刷/覆膜开胶/覆薄膜/覆opp/覆pe袋/中缝双面印刷/UV/中空圆孔等
- **修复**：PrintYsPage.jsx 中全部10行标签改为旧系统名称，字段映射保持正确（yssl1→jine1，yssl2→jine2，...，yss20→jine10）
- **影响文件**：`src/pages/PrintYsPage.jsx`

### 问题3：OrderDetailPage 印件分析空值行不显示
- **修复**：yssAnalysisRowHasValue 改为返回 true（所有行都显示，空值显示 `-`），YSS_ANALYSIS 末尾加入 UV 行 `{ label: 'UV', sl: 'yss20', je: 'jine10' }`，并移到 yssl9 之前
- **影响文件**：`src/pages/OrderDetailPage.jsx`

### 问题4：OrderEntryPage YS 印件分析标签与详情页不一致
- **修复**：YS 印件分析标签 yssl8 `贴塑`→`贴塑双(单)面`，yssl9 `切折`→`切刀打洞/圆角穿线/整理包扎`
- **影响文件**：`src/pages/OrderEntryPage.jsx`

### 构建产物
- 新 JS：`index-B3V1nwTT.js`
- 部署：10.147.19.111:3000（本地）已生效，NAS 10.147.19.81:3000 已同步
- commit：`aa25e81`（feature/user-management 分支），push 待网络恢复

---

### YM 检查与修复（2025-05-01 晚）
**commit**: `34527a0`（yinshua-admin）/ `91d0e53`（yinshua-api-node）| branch: feature/user-management

**问题1：前端 YM 工序标签顺序错位**
- 文件：`OrderEntryPage.jsx` L866-877
- 旧系统 `YMinput_Add.asp` L313-347：hzl1=烘色牢度/hzl2=切割/hzl3=超声波切割/hzl4=手工切折/hzl5=手工对折/hzl6=其它/hzl7=三角折
- 原前端错位：hzl4/hzl5/hzl6/hzl7 标签互相颠倒
- 修复：标签顺序与旧系统一致

**问题2：后端 YM INSERT 缺少 yss20（UV）**
- 文件：`order_entry.js` YM productFields
- 旧系统 `YMinput_Add.asp` L270-279：yss20（第10行UV/上光）参与总计元/只计算
- 修复：INSERT 字段新增 `yss20: ['yss20', db.mssql.NVarChar, body.yss20 || '']`

**问题3：后端 YM 缺少 zhengli UPDATE 逻辑**
- 文件：`order_entry.js` L481-494
- 旧系统 `YMinput_Add.asp` L357-362：YMGX INSERT **后**执行 UPDATE YM SET zhengli=拼接字符串
- 本地原来只有 YS 有 zhengli UPDATE，YM 缺失（ZM 本次也已修复）
- 修复：新增 YM zhengli UPDATE，标签映射 hzl1-7：烘色牢度/切割/超声波切割/手工切折/手工对折/其它/三角折

**admin.js buildProgress 补充**
- 文件：`routes/admin.js` L31 新增 `ywy_name` / L49 新增 `waifaprint`
- 用于订单详情页正确显示业务员名称和 waifaprint 值

### 构建产物
- 新 JS：`index-BA8cKoiX.js`
- 部署：10.147.19.111:3000 ✅ / 10.147.19.111:8000 ✅（后端已重启）/ NAS 10.147.19.81:3000 ✅
- commit：`34527a0`（yinshua-admin）/ `91d0e53`（yinshua-api-node），已 push ✅

---

### PrintYsPage 底部订单编号删除（2025-05-01 晚）
**commit**: `a2616c1`（yinshua-admin，feature/user-management）| 待 push（网络超时）

**修改**：底部信息区删除整行"订单编号：xxx"，保留"客户联系人/制单/生成日期"行

### 构建产物
- 新 JS：`index-DXgSo4Ml.js`
- 部署：10.147.19.111:3000 ✅ / NAS 10.147.19.81:3000 ✅
- commit：`a2616c1`（本地，已部署但待 push）

---

### 2026-05-03 YM打印页修复 + Vite代码分割优化

**1. YM打印页删除"开料要求"行**
- 文件：`src/pages/PrintYmPageDev.jsx` 第152行
- 原因：YM表（印刷面）没有`klyaoqiu`字段，旧系统YM录入页也没有开料要求
- 操作：删除 `<div className="ys-req-row"><span className="ys-cell-label">开料要求</span>...`
- 构建：`VITE_API_BASE=http://10.147.19.111:8000 npm run build`
- 同步：`serve` 进程实时读取 `/home/weii/soft/yinshua-admin/dist`，无需重启

**2. Vite构建文件过大问题**
- 根因：vite.config.js 没有配置 manualChunks，Rolldown把所有业务代码+第三方库打包成单个3.3MB的index.js
- 修复：vite.config.js 加 manualChunks 函数分chunk（Rolldown不支持对象格式，必须用函数格式）
- 结果：从1个大文件(3.3MB)→6个chunk；业务代码从3.3MB→221KB(gzip 46KB)
- 注意：vendor-misc(1.6MB)包含@antv图表库(63MB node_modules)，来自@ant-design/plots（仅统计页用到）。可后续改为动态import，但不是紧急事项

---

### 2026-05-03 PrintYmPageDev 字段修正

**1. 删除"开料要求"行**
- YM 表没有 klyaoqiu 字段，旧系统 YMinput.ASP 只有机印要求(jyyaoqiu)和工艺要求(gyyq)

**2. "工艺要求"加入"要求说明"区块**
- gyyq 字段加入机印要求同列（要求说明面板），与机印要求、备注并列

**3. "后整理工艺"区块标签全错，改正**
- 旧：工艺说明 → 整烫/烫金/压钢刀/穿线/糊纸粘合/打汽眼/其他（误用YS标签）
- 新：后整理工艺 → 烘色牢度/切割/超声波切割/三角折/手工切折/手工对折/其它（与OrderDetailPage YM_HZL_OPTIONS一致）

**4. 业务员姓名 ywy_name 列表接口修复**
- 根因：列表接口 `/api/admin/orders` 的 SQL 没有 JOIN UserInfo 表，YM 订单的 `ywy_name` 全为 null，导致打印页显示数字 ID
- 修复：在 `routes/admin.js` 第453行 SQL 加 LEFT JOIN UserInfo：`LEFT JOIN UserInfo u ON u.Userid = t.ywy`
- 影响范围：所有产品线 YS/YM/ZM/DS 的列表和详情接口（YS 详情原本已有 JOIN）

**5. 打印页基本信息行顺序调整（YM）**
- 顺序改为：用料质地 → 印刷数量 → 成品规格 → 拼数
- 删除无数据的"整烫"行（zhengli 字段 YM 全空）

**6. 打印页字段映射修正（YM）**
- 品名 → `jiagongfei`（旧系统 label "品名" 对应数据库字段 jiagongfei）
- proudnumber 实际是"印刷品号"
- 要求说明面板里工艺要求改回"工艺要求"（gyyq 字段）

**7. sydazhang 标签修正（YM）**
- 详情页：实用米数 ✅（已确认）
- 打印页价格汇总：实用大张→实用米数，单价"元/张"→"元/米"
- CSS：.ys-price-tbl td:first-child 加 white-space:nowrap + width:1% 防止换行

|**8. commit 807917a**
- YM打印页字段修正：品名/用料质地/印刷数量/成品规格/拼数顺序、sydazhang标签改为实用米数+元/米、价格表CSS防换行、ywy_name JOIN修复
- 新增 PrintYmPageDev.jsx, PrintYsPageDev.jsx, PrintYsPageDev.css, PrintZmPageDev.jsx

**9. commit ec21dcd — 引用订单状态禁止切换Tab**
- Bug：引用订单（copySource）进入新建订单页面后，可以点击 YM/ZM/DS Tab 切换类型，破坏引用关系
- 根因：`onChange={isEdit ? () => {} : setActiveProduct}` 和 `disabled: isEdit` 只判断 isEdit，未判断 copySource
- 修复：
  - `Tabs onChange` 改为 `isEdit || copySource ? () => {} : setActiveProduct`
  - 4个Tab的 `disabled` 改为 `isEdit || copySource`（共4处）
- 影响文件：`src/pages/OrderEntryPage.jsx`
- 部署：`10.147.19.111:3000` ✅ / `10.147.19.81:3000` ✅
